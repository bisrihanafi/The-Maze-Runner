using System;
using System.Collections;


using UnityEngine;
using UnityEngine.SceneManagement;


using Photon.Pun;
using Photon.Realtime;


namespace Hanafi
{
    public class CreateObject : MonoBehaviourPunCallbacks
    {
        /*
        #region Public Fields
        [Tooltip("Object anything")]
        public GameObject object1;
        [Tooltip("Object anything")]
        public GameObject object2;
        #endregion
        void Start()
        {

            if (object1 != null && object2 != null)
            {
                //PhotonNetwork.InstantiateSceneObject(this.object1.name, new Vector3(0, 0, 0), Quaternion.identity, 0);
                //PhotonNetwork.InstantiateSceneObject(this.object2.name, new Vector3(0, 0, 0), Quaternion.identity, 0);

            }
            else
            {
                Debug.LogError("<Color=Red><a>Missing</a></Color> Bola Reference. Please set it up in GameObject 'Game Manager'", this);


            }

        }
        
        */
    }
}
